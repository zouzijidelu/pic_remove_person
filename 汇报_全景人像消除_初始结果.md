# 全景人像消除 · 初始结果汇报

| 项目 | 内容 |
|------|------|
| 汇报对象 | 领导审阅 |
| 汇报日期 | 2026-08-10 |
| 阶段 | 技术 Demo / 可行性验证 |
| 当前状态 | **可本地演示，样例已跑通** |

---

## 一、背景与目标

房源全景图中常出现拍摄人员、路人等干扰，影响对外展示与后续加工。本阶段目标是验证：

> **在业务全景图上，能否以较低人工成本完成「人像定位 → 消除 → 背景补全」，并保持原图分辨率。**

结论先行：**可行。** 已完成可交互 Demo，并在多张真实业务全景样例上完成消除验证。

---

## 二、方案简述

采用「半自动分割 + 图像修复」两段式流程：

```
全景原图
   │
   ▼
SAM 点选（可多点 / 支持负点）→ 粗 Mask
   │
   ▼
笔刷精修（可选）
   │
   ▼
LaMa 空洞修复（可降采样推理）
   │
   ▼
仅 Mask 区域羽化贴回 → 输出原分辨率结果
```

| 环节 | 技术 | 作用 |
|------|------|------|
| 人像分割 | Meta SAM（ViT-B） | 点击人像自动出 Mask，减少手工涂抹 |
| Mask 精修 | Gradio 画板笔刷/橡皮 | 边界不准时可微调 |
| 背景补全 | LaMa Inpainting | 消除人像并填充合理背景 |
| 交互界面 | Gradio Web | 上传、点选、预览、一键消除 |

**产品化价值：** 操作路径短——「点一下人 → 开始消除」，适合后续嵌入房源图片处理链路。

---

## 三、当前交付物

| 交付项 | 说明 |
|--------|------|
| 本地 Demo | `./setup.sh` + `./start.sh`，浏览器访问 `http://127.0.0.1:7860` |
| 核心能力 | SAM 点选粗 Mask、笔刷精修、Mask 膨胀、推理边长可调、原分辨率输出 |
| 样例数据 | `resource/` 业务全景样例（5 张） |
| 验证结果 | `output/` 对应消除结果（分辨率与原图一致） |

---

## 四、验证结果（样例）

已在真实房源全景上完成多轮验证，**输出分辨率与原图一致**，人像区域可被消除并由模型补全背景。

| 样例 | 原图尺寸 | 验证结论 |
|------|----------|----------|
| `44290365location_02` | 5376×2688 | 可跑通完整流程 |
| `44290365location_07` | 5376×2688 | 可跑通完整流程 |
| `504984922location_05` | 6720×3360 | 大图可用，结果保持原分辨率 |
| `505018258location_10` | 6720×3360 | 场景内人像可消除，背景衔接可用 |
| `826163191location_04` | 5376×2688 | 门口/厨房区域人像可消除 |

**效果定性（Demo 阶段）：**

- **可用性：** 多数样例「点选 1～数次 + 可选笔刷」即可得到可展示级结果  
- **分辨率：** 推理可降采样以控内存，最终仅在 Mask 区域贴回，**不损失原图像素规格**  
- **局限：** 复杂遮挡、极端透视、大面积人像时，局部纹理可能略糊或需二次精修 Mask  

建议演示时对比：`resource/`（原图）与 `output/`（结果）。

---

## 五、工程现状

| 维度 | 现状 |
|------|------|
| 成熟度 | 本地可演示 Demo，非生产服务 |
| 运行环境 | Python 3.12；默认 CPU；Mac 可用 |
| 设备策略 | LaMa 默认 CPU（避免 MPS 块状伪影）；SAM 可试 MPS 加速 |
| 默认推理最长边 | 1536（Mac 内存友好；可调至 2048） |
| 模型体积 | LaMa ≈196MB；SAM ViT-B ≈375MB（首次自动下载） |

---

## 六、风险与边界（需知悉）

1. **非全自动：** 当前依赖人工点选（半自动），尚无「零点击批量过图」。  
2. **效果依赖 Mask：** Mask 不准会直接影响修复质量；复杂场景建议笔刷微调。  
3. **算力与耗时：** CPU 下大图较慢；生产环境建议 GPU。  
4. **全景特殊性：** 等距柱状投影下，边缘扭曲、跨缝人像仍可能是难点。  
5. **合规：** 仅用于房源展示去干扰，不涉及身份识别或追踪。

---

## 七、下一步建议

| 优先级 | 事项 | 预期收益 |
|--------|------|----------|
| P0 | 扩大样例集（多户型 / 多光线 / 多人）并做效果分级 | 量化「可用率」供业务决策 |
| P1 | 评估 GPU 服务化与批量接口 | 支撑产线吞吐 |
| P1 | 尝试自动人像检测（检测框 → SAM） | 降低人工点选成本 |
| P2 | 与现有房源图片流水线对接（上传前/入库前） | 形成业务闭环 |
| P2 | 对比更强修复模型或商业 API | 质量与成本权衡 |

---

## 八、请领导拍板

1. **方向是否继续：** 基于当前 Demo，是否进入「效果量化 + 产线接入」下一阶段？  
2. **资源投入：** 是否安排 GPU 环境与更多真实全景样本做评测？  
3. **验收标准：** 建议约定「可用率 / 人工干预时长 / 单张耗时」三项作为下一阶段 KPI。

---

**附件路径（本机项目内）：**

- 说明文档：`README.md`  
- 原图样例：`resource/`  
- 消除结果：`output/`  
- 启动方式：`./setup.sh` → `./start.sh`
